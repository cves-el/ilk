import React from 'react';
import { Link } from 'react-router-dom';

function Sortierung() {
    return (
        <div>
            <h1>Sortierung</h1>
            <form>
                <Link to="/">Zurück</Link>

            </form>
        </div>
    );
}

export default Sortierung;